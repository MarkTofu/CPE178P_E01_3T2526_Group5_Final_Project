# Training photos — too large and personal, not needed to run the web app
data/

# SavedModel — large binary, can be regenerated by retraining
model/saved_model/

# Python cache
__pycache__/
*.pyc

# OS clutter
.DS_Store
Thumbs.db
